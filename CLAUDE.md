@AGENTS.md
- Do not run or kill the dev server unless explicitly instructed.

- When implementing Figma designs: if a layer states or appears to use absolute or fixed positioning, try to use flex or grid for layout first; only use absolute or fixed when the same thing is not possible to achieved using flex or grid or doesn't make sense to use flex or grid in that scenario.

- Always use these design breakpoints: `ipad`, `desktop-sm`, `wide-lg`, `ultrawide`.

- If your Figma source includes a mask group, always confirm the approach for integrating the mask group with me before implementing it.