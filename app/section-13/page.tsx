import type { Metadata } from "next";
import { Section13Hero } from "./components/ui/section-13-hero";

export const metadata: Metadata = {
  title: "Spin Into the World of Top Artists.",
  description:
    "Experience music through the artists behind every hit, every genre, and every unforgettable moment.",
};

const Section13 = () => <Section13Hero />;

export default Section13;
