import type { Metadata } from "next";
import { Section23Hero } from "./component/ui/section-23-hero";

export const metadata: Metadata = {
  title: "Global Audience — Translate speech into 150+ languages",
  description:
    "Translate speech into 150+ languages with AI voices that sound natural, and are ready in minutes.",
};

const Section23 = () => <Section23Hero />;

export default Section23;
