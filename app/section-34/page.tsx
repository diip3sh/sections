import type { Metadata } from "next";

import { Section34Hero } from "./components/ui/section-34-hero";

export const metadata: Metadata = {
  title: "Voice AI — Your Front Desk, Powered by AI",
  description:
    "Custom AI voice agents that answer every call, schedule appointments, and support patients around the clock, without increasing headcount.",
};

const Section34 = () => <Section34Hero />;

export default Section34;
