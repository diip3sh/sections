import type { Metadata } from "next";

import { Section38Hero } from "./components/ui/section-38-hero";

export const metadata: Metadata = {
  title: "Get Early Access Before Everyone Else",
  description:
    "Join the waitlist to be among the first to experience the platform. Get exclusive early access, product updates, and launch-only perks.",
};

const Section38 = () => <Section38Hero />;

export default Section38;
