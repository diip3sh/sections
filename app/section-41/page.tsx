import type { Metadata } from "next";
import { Section41Hero } from "./components/ui/section-41-hero";

export const metadata: Metadata = {
  title: "Procura AI — Every Purchase Starts With a Conversation",
  description:
    "The intelligent procurement assistant that captures purchase requests, without losing control.",
};

const Section41 = () => <Section41Hero />;
export default Section41;
