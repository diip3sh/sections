import type { Metadata } from "next";

import { Section42Hero } from "./components/ui/section-42-hero";

export const metadata: Metadata = {
  title: "Wait — Get Early Access to Game Changing AI",
  description:
    "Join the exclusive group to experience Wait and create a buzz with our viral waitlist. Sign up now to get notified when we go live!",
};

const Section42 = () => <Section42Hero />;

export default Section42;
