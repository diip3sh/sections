import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Replex — Waitlist",
  description:
    "Be first to experience Replex. Join our waitlist for exclusive benefits and revolutionary automation solutions.",
};

export default function Section9Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
